{
  "name": "abyssal-toxin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate": "node scripts/validate-story.mjs",
    "build": "node scripts/validate-story.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
